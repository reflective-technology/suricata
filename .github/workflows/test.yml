name: Image Test
on:
  workflow_run:
    workflows: [ "Pull Request Build" ]
    types:
      - completed
jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository }}:${{ github.ref }}
    steps:
      - name: Run tests
        run: |
          echo "Running tests..."
          # Add your test commands here, for example:
          bash tests/test.sh
